import type { Metadata } from "next";
import AxiomApp from "./AxiomApp";

export const metadata: Metadata = {
  title: "Axiom — Intelligence Designed To Evolve",
};

export default function Page() {
  return <AxiomApp />;
}
